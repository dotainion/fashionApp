class Routes{
    home = "/home";
    login = "/login";
    recover = "/recover";
    register = "/register";
    inventory = "/inventory";
    checkout = "/checkout";
    pendingorder = "/pending";
}

export const routes = new Routes();